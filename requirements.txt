# Quality dependencies (merged from requirements-service, requirements-lint, requirements-security, requirements-test, requirements-quality)
# Install with: pip install -r requirements-quality.txt
#
# This file includes all development and quality dependencies:
# - Linting (ruff, mypy, pylint, semgrep)
# - Quality analysis (radon, vulture, autoflake)
# - Security (bandit, safety, pip-audit, semgrep)
# - Testing (pytest, pytest-cov, pytest-xdist)

tabulate>=0.9.0

# Linting
ruff>=0.5.0
mypy>=1.0.0
semgrep>=1.0.0
pylint>=3.0.0

# Code quality analysis
radon>=6.0.0
vulture>=2.0.0
autoflake>=2.0.0

# Security
bandit>=1.7.0
safety>=3.0.0
pip-audit>=2.7.0

# Testing
pytest>=8.3.0
pytest-cov>=4.1.0
pytest-xdist>=3.5.0
